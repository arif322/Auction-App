import 'package:flutter/material.dart';

class DesboardPage extends StatelessWidget {
  const DesboardPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Deshboard"),
      ),
    );
  }
}
